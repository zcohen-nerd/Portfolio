# Surfer Fleet Draft

This material was removed from the public portfolio because it is unfinished placeholder content.

It is preserved here for a future complete rewrite. Do not restore it to the public site without replacing the filler copy, placeholder assets, incomplete artifacts, and unverified claims.

The former public route `/projects/surfer-fleet/` redirects to `/projects/` (see `docusaurus.config.js`).
