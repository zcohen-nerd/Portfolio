# Portfolio Batch A Remediation Report

## Scope

Visible public breakage and credibility fixes only.

## Surfer Fleet

### Public References Removed

- Homepage Featured Systems bullet (`src/pages/index.md`)
- Projects index card (`src/pages/projects/index.md`)
- Prev/next links on SENTRY and SPARK pages (now link to each other)
- README featured-projects bullet

The shared Navbar/Footer never listed Surfer Fleet (they list ecosystem sites, not portfolio sub-pages), so no navigation changes were needed.

### Draft Preservation Location

`drafts/projects/surfer-fleet/surfer-fleet.md` — original text, placeholder references, and TBD notes preserved verbatim; `drafts/` is not routed by Docusaurus. A README in that directory explains the unpublish and the rewrite requirement.

### Redirect Result

`@docusaurus/plugin-client-redirects@3.10.1` (pinned to the core version) redirects `/projects/surfer-fleet/` → `/projects/`. Verified in the served build: navigation lands on the Projects index, no draft content flashes, no loop.

### Sitemap / Indexing Result

`build/sitemap.xml` contains zero references to the old route. The generated redirect page is not advertised as content.

## Teaching and Literacy for Kids

### Local Asset Added

`static/img/teaching/literacy-for-kids-hero.jpg` (800×533, 167 KB) — downscaled from the canonical social image of the live site (`https://www.literacy-for-kids.com/img/og-image.png`; the original 1536×1024 / 2.8 MB source). No local copy existed in this workspace and the old GitHub Pages asset is 404.

### Dead Links Replaced

- Both Teaching pages now use the local hero image
- Site links → `https://www.literacy-for-kids.com/` (the `www` form matches the established canonical in the brand registry)
- The GitHub Source link (`zcohen-nerd/computer_literacy_for_kids`) was **kept**: the repo is active (pushed July 2026) and is the actual 18-week curriculum this page describes — not obsolete.

### Route Validation

`/teaching/` and `/teaching/instructional-design/` load; both images serve HTTP 200 locally; no `zcohen-nerd.github.io/computer_literacy_for_kids` references remain in rendered output.

## Portfolio Social Card

### Image Path

`static/img/og-zac-cohen-portfolio.png`, generated by `scripts/generate-og-image.ps1` (zero-dependency GDI+, adapted from the hub's generator, self-contained in this repo).

### Dimensions

1200 × 630, verified.

### Metadata Result

`themeConfig.image` updated; built HTML emits `og:image` → `https://portfolio.zcohen-nerd.com/img/og-zac-cohen-portfolio.png` and `twitter:card` → `summary_large_image`. The small monogram is no longer the large preview.

## Writing Cadence

### Copy Changed

`src/pages/documentation/selected-essays.md`: "Every six weeks I publish…" → "I publish occasional long-form essays…"; the "Cadence: Approximately every six weeks" metadata segment removed; status now "Published occasionally". No frequency claim remains anywhere in the build.

### Substack Link Result

Both essay-page links normalized to the canonical `https://zcohennerd.substack.com/` (one was the `substack.com/@zcohennerd` profile form). Nothing links back to LinkedIn articles.

## Build Result

Success. The only broken-link warnings are the 17 pre-existing static-asset false positives (space-containing image paths the route checker can't see; all files verified present in `build/`).

## Browser Validation

Served build checked: homepage (no Surfer, 3 featured systems, correct OG metadata), projects index (3 cards), redirect route, both Teaching routes, essays route. No console errors.

## Remaining Manual Checks

- Visual/mobile spot-check in a real browser (the automation pane could not composite frames this session; all DOM/HTTP checks passed and no layout-affecting changes were made)
- After deploy: re-scrape the domain in LinkedIn Post Inspector / social debuggers to refresh the cached card

## Deferred Work

- Complete Surfer Fleet rewrite and relaunch
- Per-project social share cards
- Broader portfolio performance optimization (e.g., `ent-260.png` is 2.7 MB)
- Batch B credibility and metadata work
